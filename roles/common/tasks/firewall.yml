---
# ufw on Debian, firewalld on Fedora and RHEL. Both end up deny-by-default
# with SSH open and the cluster ports reachable only from cluster_network.
- name: Configure the host firewall for this distribution
  ansible.builtin.include_tasks: "firewall-{{ ansible_os_family }}.yml"
