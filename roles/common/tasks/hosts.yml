---
- name: Write /etc/hosts entries for cluster members
  ansible.builtin.lineinfile:
    path: /etc/hosts
    regexp: "^{{ item.ip }}\\s+"
    line: "{{ item.ip }} {{ item.name }}"
    state: present
  loop: "{{ cluster_nodes }}"
